@extends('laravel-bootstrap::layouts.interface-new')

@section('title')
    Create New View
@stop

@section('heading')
    <h1>Create New View</h1>
@stop

@section('form-items')

    <div class="form-group">
        {{ Form::label( "title" , 'View Title' , array( 'class'=>'col-lg-2 control-label' ) ) }}
        <div class="col-lg-10">
            {{ Form::text( "title" , Input::old( "title" ) , array( 'class'=>'form-control' , 'placeholder'=>'Post Title' ) ) }}
        </div>
    </div>
    <div class="form-group">
        {{ Form::label( "key" , 'View Key' , array( 'class'=>'col-lg-2 control-label' ) ) }}
        <div class="col-lg-10">
            {{ Form::text( "key" , Input::old( "key" ) , array( 'class'=>'form-control' , 'placeholder'=>'View Key' ) ) }}
        </div>
    </div>
@stop